import 'package:flutter/material.dart';
import 'package:ticket_app/utils/global_widgets.dart';

class UpcomingFlightsPage extends StatelessWidget {
  const UpcomingFlightsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: reusableText("title"),);
  }
}
